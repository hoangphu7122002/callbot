apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f /home/hoangphu7122002/callbot/docker-compose.yml
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: worker
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: worker
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f /home/hoangphu7122002/callbot/docker-compose.yml
        kompose.version: 1.34.0 (cbf2835db)
      labels:
        io.kompose.service: worker
    spec:
      containers:
        - name: worker
          image: hoangphupickleball/callbot-worker:latest
          imagePullPolicy: Never
          resources:
            limits:
              cpu: "1"
              memory: "1073741824"
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /app/records1
              name: records-volume
            - mountPath: /app/.env
              name: worker-cm1
              subPath: .env
      # restartPolicy: Never
      imagePullSecrets:
      - name: regcred
      volumes:
        - name: records-volume
          persistentVolumeClaim:
            claimName: records-pvc-new
        - configMap:
            items:
              - key: .env
                path: .env
            name: worker-cm1
          name: worker-cm1
